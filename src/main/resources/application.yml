spring:
  security:
    user:
      name: ${SECURITY_USER_NAME:admin}
      password: ${SECURITY_USER_PASSWORD:admin}

  application:
    name: alojamientos

  datasource:
    url: ${PGDATABASE}
    driver-class-name: org.postgresql.Driver
    hikari:
      maximum-pool-size: 10
      minimum-idle: 2
      connection-timeout: 30000

  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
        show_sql: false
    open-in-view: false

server:
  port: ${PORT:8080}

# Configuración de Cloudinary
cloudinary:
  cloud-name: ${CLOUDINARY_CLOUD_NAME:dygl0ond3}
  api-key: ${CLOUDINARY_API_KEY:355884264733534}
  api-secret: ${CLOUDINARY_API_SECRET:6Ty9-KX9qPI0YpnINa5yPVfn06A}

# JWT Configuration
jwt:
  secret: ${JWT_SECRET:mySecretKeyForJwtTokenGenerationThatShouldBeVeryLongAndSecure123456789ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz}
  expiration: 8640000

# Logging
logging:
  level:
    com.avanzada.alojamientos: INFO
    org.hibernate.SQL: WARN
    org.hibernate.type.descriptor.sql.BasicBinder: WARN

mercadopago:
  access:
    token: ${MERCADOPAGO_ACCESS_TOKEN:APP_USR-6031776921093655-100900-11e4ceef9cd55ec049276ca19a7f8a81-2914163040}